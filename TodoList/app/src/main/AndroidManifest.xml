<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.rodrigo.todolist" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".adapter.ActivityMain"
            android:label="@string/app_name" >
        </activity>
        <activity
            android:name=".adapter.ActivityLogin"
            android:label="@string/app_name" >
        </activity>
        <activity
            android:name=".adapter.CadUsuarioActivity"
            android:label="@string/title_activity_cad_usuario" >
        </activity>
        <activity
            android:name=".adapter.TelaInicio"
            android:label="@string/title_activity_tela_inicio" >

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
    </application>

</manifest>
